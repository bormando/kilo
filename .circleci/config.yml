version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
      - image: selenium/standalone-chrome:latest
    steps:
      - checkout
      - run: npm install
      - run: npm run test